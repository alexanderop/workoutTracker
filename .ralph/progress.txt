=== Ralph Wiggum Progress Log ===
Started: 2026-01-06

## PRD: Lint Warnings Cleanup

### Overview
Fix 24 ESLint warnings:
- 2x no-restricted-syntax (querySelector usage in tests)
- 22x complexity (functions exceeding max 10)

### Features (11 total)
1. testing - Replace querySelector in test files
2. composables - useVersionCheck complexity
3. database - exerciseProgress complexity (3 functions)
4. composables - useBenchmarkForm complexity (2 functions)
5. composables - useExerciseProgress complexity
6. composables - usePastWorkout complexity
7. ui - StandaloneTimerRunner complexity
8. composables - useWorkout complexity (2 functions)
9. utilities - markdownImport complexity (7 functions)
10. utilities - fractionalIndexing complexity (4 functions)
11. verification - Final lint check

---

## Session: 2026-01-06 22:37

### Feature 1: testing - Replace querySelector in test files ✅

**Changes made:**
1. `src/__tests__/helpers/pages/BenchmarkFormPO.ts`:
   - Converted `getExerciseItems()` from sync to async
   - Replaced `document.querySelectorAll('button[aria-label="Remove exercise"]')` with Vitest locator chain:
     - First get exercise items by test ID: `page.getByTestId('benchmark-exercise-item').all()`
     - Then scope `getByRole('button', { name: /remove exercise/i })` to each item
   - Updated `removeExercise()` to await the now-async `getExerciseItems()`

2. `src/__tests__/integration/home-delete-workout.spec.ts`:
   - Replaced `swipeable.querySelector('[data-testid="swipeable-content"]')` with `page.getByTestId('swipeable-content').all()` and iteration

**Verification:**
- `pnpm type-check` - PASS
- `pnpm lint` - no-restricted-syntax warnings eliminated (22 complexity warnings remain for next features)
- `pnpm test` - 86 test files, 783 passed tests

**Note for next session:**
- 22 complexity warnings remain (10 features left)
- Start with feature 2 (useVersionCheck complexity) as it's the simplest single-function fix

---

## Session: 2026-01-06 22:45

### Feature 2: composables - useVersionCheck complexity ✅

**Changes made:**
1. `src/composables/useVersionCheck.ts`:
   - Extracted `isObject()` helper function to check if value is a non-null object
   - Extracted `hasStringField()` helper function to validate string properties
   - Refactored `isVersionInfo()` to use these helpers, reducing complexity from 12 to ≤10
   - The `tag` field validation (string or null) remains inline as it's unique

**Verification:**
- `pnpm type-check` - PASS
- `pnpm test` - 86 test files, all tests pass
- `pnpm lint` - complexity warning for useVersionCheck is eliminated (21 warnings remain)

**Note for next session:**
- 21 complexity warnings remain (9 features left)
- Next candidate: Feature 3 (exerciseProgress repository - 3 functions) or Feature 5 (useExerciseProgress - single function, simpler)
