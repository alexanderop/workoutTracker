---
name: ship
description: End-to-end delivery: commit staged changes, push to remote, and open a pull request in one flow (runs /push then /pr). Use when the user says "ship it", "ship this", "ship the changes", or wants to go from working tree to open PR in one step.
---

# Ship

Execute `/push` to commit and push changes, then execute `/pr` to open a pull request.
