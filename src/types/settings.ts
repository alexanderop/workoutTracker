/**
 * Shared settings types.
 *
 * These types define user preferences used across the application.
 */

export type WeightUnit = 'kg' | 'lbs'
export type HeightUnit = 'cm' | 'ft-in'
export type Language = 'en' | 'de'
