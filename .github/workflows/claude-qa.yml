# Consolidated Claude QA Workflow
# Replaces both claude-playwright-explore.yml and claude-qa-verify.yml

name: Claude QA

on:
  workflow_dispatch:
    inputs:
      mode:
        description: 'QA mode'
        type: choice
        options:
          - explore
          - verify
        default: 'explore'
      pr_number:
        description: 'PR number (for posting results)'
        required: false
        type: string
      focus:
        description: 'Test focus area'
        type: choice
        options:
          - general
          - navigation
          - forms
          - workout-flow
        default: 'general'
  pull_request:
    types: [labeled]
  issue_comment:
    types: [created]

jobs:
  qa:
    # Run on: manual trigger, specific labels, or @claude verify comment
    if: |
      github.event_name == 'workflow_dispatch' ||
      (github.event_name == 'pull_request' &&
       contains(fromJson('["claude-explore", "qa-verify"]'), github.event.label.name)) ||
      (github.event_name == 'issue_comment' &&
       github.event.issue.pull_request &&
       contains(github.event.comment.body, '@claude verify'))

    runs-on: ubuntu-latest
    timeout-minutes: 20

    # Prevent duplicate runs on same PR (use ref for workflow_dispatch branch deduplication)
    concurrency:
      group: claude-qa-${{ github.event.pull_request.number || github.event.issue.number || github.ref }}
      cancel-in-progress: true

    permissions:
      contents: read
      pull-requests: write
      issues: write
      id-token: write
      statuses: write

    env:
      NODE_VERSION: '22'
      QA_FOCUS: ${{ github.event.inputs.focus || 'general' }}

    steps:
      - name: Determine QA mode
        id: qa-mode
        shell: bash
        run: |
          MODE="explore"
          if [[ "${{ github.event.label.name }}" == "qa-verify" ]]; then
            MODE="verify"
          elif [[ "${{ github.event.label.name }}" == "claude-explore" ]]; then
            MODE="explore"
          elif [[ "${{ github.event_name }}" == "issue_comment" ]]; then
            MODE="verify"
          elif [[ -n "${{ github.event.inputs.mode }}" ]]; then
            MODE="${{ github.event.inputs.mode }}"
          fi
          echo "mode=$MODE" >> $GITHUB_OUTPUT
          echo "QA_MODE=$MODE" >> $GITHUB_ENV
          echo "Determined QA mode: $MODE"

      - name: Get PR ref for issue_comment
        if: github.event_name == 'issue_comment'
        id: pr-ref
        uses: actions/github-script@v7
        with:
          script: |
            const { data: pr } = await github.rest.pulls.get({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: context.payload.issue.number
            });
            core.setOutput('ref', pr.head.sha);

      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: ${{ steps.pr-ref.outputs.ref || github.event.pull_request.head.sha || github.sha }}

      # Reuse existing composite action instead of duplicating setup
      - name: Setup test environment
        uses: ./.github/actions/setup-test-env
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Get PR context
        if: env.QA_MODE == 'verify'
        id: pr-context
        uses: actions/github-script@v7
        env:
          # Avoid script injection by passing inputs via environment variables
          PR_NUMBER_INPUT: ${{ github.event.inputs.pr_number }}
        with:
          script: |
            let prNumber, prBody, prTitle;

            if (context.eventName === 'pull_request') {
              prNumber = context.payload.pull_request.number;
              prBody = context.payload.pull_request.body || '';
              prTitle = context.payload.pull_request.title;
            } else if (context.eventName === 'issue_comment') {
              // @claude verify comment on PR
              prNumber = context.payload.issue.number;
              const { data: pr } = await github.rest.pulls.get({
                owner: context.repo.owner,
                repo: context.repo.repo,
                pull_number: prNumber
              });
              prBody = pr.body || '';
              prTitle = pr.title;
            } else if (process.env.PR_NUMBER_INPUT) {
              prNumber = parseInt(process.env.PR_NUMBER_INPUT, 10);
              const { data: pr } = await github.rest.pulls.get({
                owner: context.repo.owner,
                repo: context.repo.repo,
                pull_number: prNumber
              });
              prBody = pr.body || '';
              prTitle = pr.title;
            }

            // Fetch linked issues
            let linkedIssues = '';
            if (prBody) {
              const matches = prBody.match(/(?:closes|fixes|resolves)\s+#(\d+)/gi) || [];
              for (const match of matches) {
                const issueNum = match.match(/\d+/)[0];
                try {
                  const { data: issue } = await github.rest.issues.get({
                    owner: context.repo.owner,
                    repo: context.repo.repo,
                    issue_number: parseInt(issueNum)
                  });
                  linkedIssues += `\n### Issue #${issueNum}: ${issue.title}\n${issue.body || ''}\n`;
                } catch (e) {}
              }
            }

            core.setOutput('pr_number', prNumber || '');
            core.setOutput('pr_title', prTitle || '');
            core.setOutput('pr_body', prBody || '');
            core.setOutput('linked_issues', linkedIssues);

      - name: Start development server
        shell: bash
        run: |
          pnpm dev --host 0.0.0.0 --port 5173 &
          DEV_PID=$!
          echo "DEV_PID=$DEV_PID" >> $GITHUB_ENV

          # Early failure detection - check process is still alive after startup
          sleep 2
          if ! kill -0 "$DEV_PID" 2>/dev/null; then
            echo "Dev server failed to start immediately"
            exit 1
          fi

          echo "Waiting for dev server with app mounted..."
          for i in {1..90}; do
            # Check for HTTP 200 AND expected content (more robust pattern)
            if curl -sf http://localhost:5173 | grep -qE '(id="app"|<title>)'; then
              echo "Dev server ready!"
              exit 0
            fi
            # Also check process is still running during wait
            if ! kill -0 "$DEV_PID" 2>/dev/null; then
              echo "Dev server process died during startup"
              exit 1
            fi
            sleep 1
          done
          echo "Dev server failed to start properly"
          curl -v http://localhost:5173 || true
          exit 1

      - name: Run Claude QA - Explore Mode
        if: env.QA_MODE == 'explore'
        id: claude-explore
        uses: anthropics/claude-code-action@v1
        with:
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          prompt: |
            You are a senior QA engineer performing exploratory testing.
            App URL: http://localhost:5173

            ## Budget: 60 turns total
            - Turns 1-5: Quick navigation smoke test
            - Turns 6-45: Feature testing based on focus
            - Turns 46-55: Edge cases, mobile viewport test
            - Turns 56-60: WRITE REPORT (mandatory!)

            ## Focus: ${{ env.QA_FOCUS }}

            **Test Plan by Focus:**
            - general: All pages load → Exercise CRUD → Workout flow → Settings
            - navigation: Every nav link, back buttons, deep links, breadcrumbs
            - forms: Exercise form validation, workout setup, settings forms
            - workout-flow: Full workout from start to complete with all features

            ## Automatic FAIL criteria:
            - Any uncaught JavaScript error in console
            - Any blank/white screen
            - Buttons that don't respond
            - 404 or network errors

            ## Mobile Test (use browser_resize)
            Resize to 375x667 and verify:
            - Navigation works
            - No horizontal scroll
            - Forms are usable

            ## If you find bugs:
            Take a screenshot with name format: `bug-[description].png` (e.g., bug-broken-nav.png)

            ## FINAL STEP (turns 56-60):
            Write `qa-report.md`:

            ```markdown
            # QA Exploration Report

            **Date**: [today] | **Focus**: ${{ env.QA_FOCUS }} | **Tester**: Claude QA

            ## Summary
            [1 sentence: overall health of the app]

            ## Test Results
            | Test | Status | Notes |
            |------|--------|-------|
            | ... | ✅/❌ | ... |

            ## Mobile Responsive
            | Check | Status |
            |-------|--------|
            | Navigation | ✅/❌ |
            | Forms | ✅/❌ |
            | No horizontal scroll | ✅/❌ |

            ## Bugs Found
            [List with screenshots, or "None"]

            ## Console Errors
            [List errors, or "None"]

            ## Verdict
            **[✅ HEALTHY / ⚠️ MINOR ISSUES / ❌ CRITICAL BUGS]**
            ```

          claude_args: |
            --max-turns 60
            --mcp-config '{"mcpServers":{"playwright":{"command":"npx","args":["@playwright/mcp@latest","--headless","--isolated","--caps=vision,testing,tracing"]}}}'
            --allowedTools "mcp__playwright__browser_navigate,mcp__playwright__browser_navigate_back,mcp__playwright__browser_click,mcp__playwright__browser_type,mcp__playwright__browser_snapshot,mcp__playwright__browser_take_screenshot,mcp__playwright__browser_console_messages,mcp__playwright__browser_wait_for,mcp__playwright__browser_hover,mcp__playwright__browser_resize,mcp__playwright__browser_drag,mcp__playwright__browser_select_option,mcp__playwright__browser_press_key,mcp__playwright__browser_evaluate,mcp__playwright__browser_network_requests,mcp__playwright__browser_tabs,mcp__playwright__browser_file_upload,mcp__playwright__browser_fill_form,mcp__playwright__browser_handle_dialog,mcp__playwright__browser_close,mcp__playwright__browser_run_code,mcp__playwright__browser_install,mcp__playwright__browser_mouse_click_xy,mcp__playwright__browser_mouse_drag_xy,mcp__playwright__browser_mouse_move_xy,mcp__playwright__browser_generate_locator,mcp__playwright__browser_verify_element_visible,mcp__playwright__browser_verify_list_visible,mcp__playwright__browser_verify_text_visible,mcp__playwright__browser_verify_value,mcp__playwright__browser_start_tracing,mcp__playwright__browser_stop_tracing,Read,Write"

      - name: Run Claude QA - Verify Mode
        if: env.QA_MODE == 'verify'
        id: claude-verify
        uses: anthropics/claude-code-action@v1
        with:
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          prompt: |
            You are a senior QA engineer who TRIES TO BREAK THINGS. Your job is NOT just to verify happy paths - it's to find bugs before users do.

            App URL: http://localhost:5173

            ## PR Context
            **PR #${{ steps.pr-context.outputs.pr_number }}**: ${{ steps.pr-context.outputs.pr_title }}

            ### Description:
            ${{ steps.pr-context.outputs.pr_body }}

            ### Linked Issues:
            ${{ steps.pr-context.outputs.linked_issues }}

            ## QA Mindset - Think Like a Tester

            For EACH feature you test, ask yourself:
            - What happens with empty input?
            - What happens with invalid input?
            - What happens if I click rapidly?
            - What happens if I navigate away mid-action?
            - Does it work on mobile (375x667)?

            ## Budget: 50 turns
            - Turns 1-3: Parse requirements from PR description
            - Turns 4-20: Test happy paths for each requirement
            - Turns 21-35: TRY TO BREAK IT - edge cases, invalid inputs, rapid clicks
            - Turns 36-42: Mobile viewport test (resize to 375x667)
            - Turns 43-50: WRITE REPORT

            ## Breaking Things - Be Adversarial

            Test these for any input fields or interactive elements:
            - Empty inputs: Submit forms with nothing filled in
            - Boundary values: 0, -1, 999999, very long strings (100+ chars)
            - Rapid interactions: Click same button 5 times fast
            - Cancel mid-flow: Start an action, then navigate away or close dialog
            - Special characters: Try emojis 🔥, quotes "test", angle brackets <test>

            ## Bug Severity Guide
            - 🔴 Critical: App crashes, data loss, security issue, blank screen
            - 🟠 Major: Feature broken, blocks user from completing task
            - 🟡 Minor: Visual glitch, typo, confusing UX but workaround exists
            - ⚪ Suggestion: Works but could be better

            ## Mobile Testing (REQUIRED)
            Use browser_resize to test at 375x667 (iPhone SE):
            - Does layout break?
            - Are touch targets big enough?
            - Is there horizontal scrolling?
            - Can you complete the main flow?

            ## If you find bugs:
            Take screenshot with severity: `bug-[severity]-[description].png`
            Examples: `bug-major-form-not-submitting.png`, `bug-minor-text-overflow.png`

            ## FINAL STEP - Write qa-report.md:

            ```markdown
            # QA Verification Report

            **PR**: #${{ steps.pr-context.outputs.pr_number }} - ${{ steps.pr-context.outputs.pr_title }}
            **Date**: [today] | **Tester**: Claude QA

            ## Summary
            [One sentence: what was tested and overall health]

            ## Requirements Verified
            | # | Requirement | Status | How Tested |
            |---|-------------|--------|------------|
            | 1 | ... | ✅/❌ | [Specific actions taken] |

            ## Edge Cases & Negative Testing
            | Test | Result | Notes |
            |------|--------|-------|
            | Empty form submission | ✅/❌ | ... |
            | Rapid button clicks | ✅/❌ | ... |
            | Long text input | ✅/❌ | ... |
            | Special characters | ✅/❌ | ... |

            ## Mobile Testing (375x667)
            | Check | Status |
            |-------|--------|
            | Layout intact | ✅/❌ |
            | No horizontal scroll | ✅/❌ |
            | Main flow completable | ✅/❌ |

            ## Bugs Found
            | Severity | Description | Screenshot |
            |----------|-------------|------------|
            | 🔴/🟠/🟡 | ... | `bug-xxx.png` |

            (Or "None found")

            ## Console Errors
            [List or "None"]

            ## Verdict
            **[✅ APPROVED / ❌ NEEDS FIXES / ⚠️ NEEDS CLARIFICATION]**

            [One sentence summary explaining the verdict]
            ```

          claude_args: |
            --max-turns 50
            --mcp-config '{"mcpServers":{"playwright":{"command":"npx","args":["@playwright/mcp@latest","--headless","--isolated","--caps=vision,testing,tracing"]}}}'
            --allowedTools "mcp__playwright__browser_navigate,mcp__playwright__browser_navigate_back,mcp__playwright__browser_click,mcp__playwright__browser_type,mcp__playwright__browser_snapshot,mcp__playwright__browser_take_screenshot,mcp__playwright__browser_console_messages,mcp__playwright__browser_wait_for,mcp__playwright__browser_hover,mcp__playwright__browser_resize,mcp__playwright__browser_drag,mcp__playwright__browser_select_option,mcp__playwright__browser_press_key,mcp__playwright__browser_evaluate,mcp__playwright__browser_network_requests,mcp__playwright__browser_tabs,mcp__playwright__browser_file_upload,mcp__playwright__browser_fill_form,mcp__playwright__browser_handle_dialog,mcp__playwright__browser_close,mcp__playwright__browser_run_code,mcp__playwright__browser_install,mcp__playwright__browser_mouse_click_xy,mcp__playwright__browser_mouse_drag_xy,mcp__playwright__browser_mouse_move_xy,mcp__playwright__browser_generate_locator,mcp__playwright__browser_verify_element_visible,mcp__playwright__browser_verify_list_visible,mcp__playwright__browser_verify_text_visible,mcp__playwright__browser_verify_value,mcp__playwright__browser_start_tracing,mcp__playwright__browser_stop_tracing,Read,Write"

      - name: Check report exists
        id: check-report
        if: always()
        shell: bash
        run: |
          if [ -f "qa-report.md" ]; then
            echo "exists=true" >> $GITHUB_OUTPUT
            echo "Report generated successfully"
          else
            echo "exists=false" >> $GITHUB_OUTPUT
            echo "Warning: No report generated"
          fi

      # Retry with full tools if main run failed to generate a report
      - name: Retry if no report generated
        if: steps.check-report.outputs.exists == 'false'
        uses: anthropics/claude-code-action@v1
        with:
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          prompt: |
            IMPORTANT: The previous QA run did not generate a report.

            Do a quick navigation smoke test and write qa-report.md immediately:
            1. Navigate to http://localhost:5173
            2. Check 3-4 main pages load correctly
            3. Write the report with what you observed

            Write `qa-report.md` now with:
            - Summary of pages tested
            - Any errors found
            - Verdict: ✅ HEALTHY / ⚠️ MINOR ISSUES / ❌ CRITICAL BUGS
          claude_args: |
            --max-turns 15
            --mcp-config '{"mcpServers":{"playwright":{"command":"npx","args":["@playwright/mcp@latest","--headless","--isolated","--caps=vision,testing,tracing"]}}}'
            --allowedTools "mcp__playwright__browser_navigate,mcp__playwright__browser_navigate_back,mcp__playwright__browser_click,mcp__playwright__browser_type,mcp__playwright__browser_snapshot,mcp__playwright__browser_take_screenshot,mcp__playwright__browser_console_messages,mcp__playwright__browser_wait_for,mcp__playwright__browser_hover,mcp__playwright__browser_resize,mcp__playwright__browser_drag,mcp__playwright__browser_select_option,mcp__playwright__browser_press_key,mcp__playwright__browser_evaluate,mcp__playwright__browser_network_requests,mcp__playwright__browser_tabs,mcp__playwright__browser_file_upload,mcp__playwright__browser_fill_form,mcp__playwright__browser_handle_dialog,mcp__playwright__browser_close,mcp__playwright__browser_run_code,mcp__playwright__browser_install,mcp__playwright__browser_mouse_click_xy,mcp__playwright__browser_mouse_drag_xy,mcp__playwright__browser_mouse_move_xy,mcp__playwright__browser_generate_locator,mcp__playwright__browser_verify_element_visible,mcp__playwright__browser_verify_list_visible,mcp__playwright__browser_verify_text_visible,mcp__playwright__browser_verify_value,mcp__playwright__browser_start_tracing,mcp__playwright__browser_stop_tracing,Read,Write"

      - name: Verify report after retry
        id: check-report-retry
        if: steps.check-report.outputs.exists == 'false'
        shell: bash
        run: |
          if [ -f "qa-report.md" ]; then
            echo "exists=true" >> $GITHUB_OUTPUT
          else
            echo "exists=false" >> $GITHUB_OUTPUT
          fi

      - name: Set commit status
        id: qa-status
        if: always()
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            let state = 'success';
            let description = 'QA passed';

            try {
              const report = fs.readFileSync('qa-report.md', 'utf8');
              if (report.includes('❌ NEEDS FIXES') || report.includes('❌ CRITICAL')) {
                state = 'failure';
                description = 'QA found critical issues';
              } else if (report.includes('⚠️')) {
                state = 'pending';
                description = 'QA found minor issues';
              } else if (report.includes('✅ APPROVED') || report.includes('✅ HEALTHY')) {
                state = 'success';
                description = 'QA passed';
              }
            } catch (e) {
              state = 'error';
              description = 'QA report not generated';
            }

            // Use PR head sha for issue_comment, otherwise use context.sha
            const sha = '${{ steps.pr-ref.outputs.ref }}' || context.sha;

            await github.rest.repos.createCommitStatus({
              owner: context.repo.owner,
              repo: context.repo.repo,
              sha: sha,
              state: state,
              context: 'Claude QA',
              description: description,
              target_url: `${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`
            });

            // Set output for job failure
            core.setOutput('qa_state', state);

      - name: Post results to PR
        if: |
          always() &&
          (github.event.pull_request.number || github.event.issue.number || github.event.inputs.pr_number || steps.pr-context.outputs.pr_number)
        uses: actions/github-script@v7
        env:
          # Avoid script injection by passing inputs via environment variables
          PR_NUMBER_INPUT: ${{ github.event.inputs.pr_number }}
          PR_NUMBER_CONTEXT: ${{ steps.pr-context.outputs.pr_number }}
          QA_MODE: ${{ env.QA_MODE }}
        with:
          script: |
            const fs = require('fs');
            const prNumber = context.payload.pull_request?.number ||
                            context.payload.issue?.number ||
                            process.env.PR_NUMBER_INPUT ||
                            process.env.PR_NUMBER_CONTEXT;

            if (!prNumber) return;

            let qaReport = '';
            try {
              qaReport = fs.readFileSync('qa-report.md', 'utf8');
            } catch (e) {
              qaReport = '⚠️ QA report not generated. Check [workflow logs](' +
                `${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId})`;
            }

            const mode = process.env.QA_MODE === 'verify' ? 'Verification' : 'Exploration';

            const focus = process.env.QA_FOCUS || 'general';
            const runUrl = `${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`;
            const body = [
              `## 🔍 Claude QA ${mode} Report`,
              '',
              `**Mode**: ${mode} | **Focus**: ${focus}`,
              `**Run**: [View Logs](${runUrl})`,
              '',
              '---',
              '',
              qaReport,
              '',
              '---',
              '_Automated QA by Claude Code_'
            ].join('\n');

            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: parseInt(prNumber),
              body: body
            });

      - name: Upload QA artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: qa-artifacts-${{ github.run_id }}
          path: |
            qa-report.md
            bug-*.png
          if-no-files-found: ignore

      - name: Remove trigger label
        if: |
          github.event_name == 'pull_request' &&
          contains(fromJson('["claude-explore", "qa-verify"]'), github.event.label.name)
        uses: actions/github-script@v7
        env:
          LABEL_NAME: ${{ github.event.label.name }}
        with:
          script: |
            try {
              await github.rest.issues.removeLabel({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.payload.pull_request.number,
                name: process.env.LABEL_NAME
              });
            } catch (e) {
              // Only suppress 404 (label not found), warn on other errors
              if (e.status !== 404) {
                core.warning(`Failed to remove label: ${e.message}`);
              }
            }

      - name: Cleanup
        if: always()
        shell: bash
        run: kill $DEV_PID 2>/dev/null || true

      - name: Job summary
        if: always()
        shell: bash
        run: |
          echo "## Claude QA Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- **Mode**: ${{ env.QA_MODE }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Focus**: ${{ env.QA_FOCUS }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Report generated**: ${{ steps.check-report.outputs.exists }}" >> $GITHUB_STEP_SUMMARY
          echo "- **QA Result**: ${{ steps.qa-status.outputs.qa_state }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Estimated Cost" >> $GITHUB_STEP_SUMMARY
          echo "- Claude API: ~\$0.20-0.40" >> $GITHUB_STEP_SUMMARY
          echo "- GitHub Actions: ~\$0.05" >> $GITHUB_STEP_SUMMARY

      - name: Fail job if QA found issues
        if: steps.qa-status.outputs.qa_state == 'failure'
        shell: bash
        run: |
          echo "❌ QA found critical issues - failing the pipeline"
          exit 1
